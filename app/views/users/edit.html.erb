<%= form_for @user do |f| %>
  Username: <%= f.text_field :username, placeholder: 'Mac69' %><br />
  Email: <%= f.email_field :email, placeholder: 'dennis@paddyspub.com' %><br />
  Password: <%= f.password_field :password %><br />
  <%= f.submit%>
<% end %>

<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this account from being updated:</h2>

    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>